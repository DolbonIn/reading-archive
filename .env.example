# Reading Archive environment configuration
# Copy this file to `.env` and fill in the secrets before running the tools.

# Google Gemini API key
GOOGLE_API_KEY=replace-with-your-key

# Optional git automation toggles for the local Flask uploader
READING_ARCHIVE_AUTO_COMMIT=false
READING_ARCHIVE_AUTO_PUSH=false
